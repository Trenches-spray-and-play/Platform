// Re-export from shared auth package
// This file exists for backwards compatibility with existing imports
'use client';

export { AuthProvider, useAuth, type AuthContextType } from '@trenches/auth';
